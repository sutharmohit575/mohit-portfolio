# ── Copy this file to .env and fill in your real values ──────────
# NEVER commit .env to git — it contains secrets

# App
DEBUG=false
SECRET_KEY=random-32-character-string

# CORS — add your deployed domain here in production
# ALLOWED_ORIGINS=["https://yourdomain.com"]

# Email — leave blank to use console logging in dev (no email sent)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sutharmohit575@gmail.com
SMTP_PASSWORD=you app password
CONTACT_EMAIL=sutharmohit575@gmail.com

# Rate limiting (max contact form submissions per hour per IP)
CONTACT_RATE_LIMIT=5
